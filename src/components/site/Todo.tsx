// A bracketed placeholder the site owner replaces with real content.
const Todo = ({ children }: { children: React.ReactNode }) => (
    <div className="site-todo">{children}</div>
);

export default Todo;
